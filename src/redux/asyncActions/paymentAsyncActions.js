import axios from "axios";
import * as paymentActions from "../actions/paymentActions";

// Ví dụ:
// export const createDepositPayment = (bookingId, paymentMethod = "bank_transfer") => async (dispatch, getState) => {
//   // ... logic ...
// };
// ... (Các async action payment khác sẽ được thêm tiếp vào file này)
